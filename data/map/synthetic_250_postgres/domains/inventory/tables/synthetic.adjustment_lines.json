{
  "table": "adjustment_lines",
  "schema": "synthetic",
  "database": "synthetic_250_postgres",
  "description": "The `synthetic.adjustment_lines` table is designed to represent entity adjustments within a synthetic dataset, although currently, it contains no data. It is likely intended to hold detailed records of adjustments, uniquely identified by the `adjustment_line_id` primary key, with undefined relationships to other tables suggesting links to broader transactional or categorical data. The absence of column names and sample data restricts further characterization of its role or functionality within the database schema.",
  "row_count": 0,
  "columns": [
    {
      "name": "adjustment_line_id",
      "type": "integer",
      "nullable": false,
      "description": "This column represents a unique identifier for individual entries in the adjustment lines table, ensuring each record is distinct within the dataset. Purpose unclear from available data.",
      "default": "nextval('synthetic.adjustment_lines_adjustment_line_id_seq'::regclass)"
    },
    {
      "name": "adjustment_id",
      "type": "integer",
      "nullable": false,
      "description": "Represents a unique identifier for an adjustment within the system. It is used to link specific adjustment transactions to their overall record or process.",
      "default": null
    },
    {
      "name": "inventory_id",
      "type": "integer",
      "nullable": false,
      "description": "Purpose unclear from available data.",
      "default": null
    },
    {
      "name": "quantity_change",
      "type": "integer",
      "nullable": false,
      "description": "This column captures the numerical variation in amounts related to adjustments in a transactional entry. It signifies the count or units of change that occurs to an item or account balance within the adjustment process.",
      "default": null
    },
    {
      "name": "unit_cost",
      "type": "numeric",
      "nullable": true,
      "description": "Represents the cost per unit for an item or service associated with an adjustment line. Purpose unclear from available data.",
      "default": null
    },
    {
      "name": "reason",
      "type": "character varying",
      "nullable": true,
      "description": "Purpose unclear from available data.",
      "default": null
    },
    {
      "name": "created_at",
      "type": "timestamp without time zone",
      "nullable": true,
      "description": "This column records the date and time when a specific adjustment line entry was created, capturing the temporal context of data modifications. Purpose unclear from available data due to lack of sample values.",
      "default": "CURRENT_TIMESTAMP"
    },
    {
      "name": "updated_at",
      "type": "timestamp without time zone",
      "nullable": true,
      "description": "Denotes the date and time when the record was last modified. This timestamp reflects the most recent update in the system, aiding in tracking changes to adjustment entries.",
      "default": "CURRENT_TIMESTAMP"
    }
  ],
  "primary_key": [
    "adjustment_line_id"
  ],
  "foreign_keys": [
    {
      "constraint_name": "fk_adjustment_lines_adjustment_id",
      "column_name": "adjustment_id",
      "referenced_table": "synthetic.inventory_adjustments",
      "referenced_column": "adjustment_id"
    },
    {
      "constraint_name": "fk_adjustment_lines_inventory_id",
      "column_name": "inventory_id",
      "referenced_table": "synthetic.inventory_items",
      "referenced_column": "inventory_id"
    }
  ],
  "indexes": [
    {
      "index_name": "adjustment_lines_pkey",
      "index_definition": "CREATE UNIQUE INDEX adjustment_lines_pkey ON synthetic.adjustment_lines USING btree (adjustment_line_id)",
      "columns": [
        "adjustment_line_id"
      ],
      "is_unique": true
    }
  ],
  "sample_data": [],
  "generated_at": "2025-12-14T23:42:22.780Z"
}